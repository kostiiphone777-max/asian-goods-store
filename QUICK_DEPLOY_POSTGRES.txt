═══════════════════════════════════════════════════════════════
🚀 БЫСТРЫЙ ДЕПЛОЙ НА СЕРВЕР С POSTGRES (COPY & PASTE)
═══════════════════════════════════════════════════════════════

Сервер: 45.141.78.168
База данных: PostgreSQL (только Postgres, SQLite удален)

═══════════════════════════════════════════════════════════════
ВАРИАНТ 1: АВТОМАТИЧЕСКИЙ (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════

В PowerShell выполните:

.\deploy-to-server-pm2.ps1

Выберите пункт: 1 (Полное развертывание)

═══════════════════════════════════════════════════════════════
ВАРИАНТ 2: РУЧНОЙ ДЕПЛОЙ
═══════════════════════════════════════════════════════════════

ШАГ 1: На сервере - удалите старые SQLite файлы
───────────────────────────────────────────────────────────────

ssh root@45.141.78.168
cd /opt/asian-goods-store
rm -f backend/database/store.db
rm -f backend/database/schema.sql
rm -f backend/scripts/init-database.js
rm -f backend/scripts/migrate-to-sqlite.js
exit

───────────────────────────────────────────────────────────────

ШАГ 2: На вашем компьютере - создайте архив и загрузите
───────────────────────────────────────────────────────────────

# В PowerShell:
tar -czf deploy.tar.gz --exclude=node_modules --exclude=.next --exclude=backend/node_modules --exclude=backend/database/store.db --exclude=backend/database/*.db --exclude=backend/database/*.sqlite --exclude=backend/database/schema.sql --exclude=.git --exclude=logs *

scp deploy.tar.gz root@45.141.78.168:/opt/asian-goods-store/

ssh root@45.141.78.168 "cd /opt/asian-goods-store && tar -xzf deploy.tar.gz && rm deploy.tar.gz"

───────────────────────────────────────────────────────────────

ШАГ 3: На сервере - настройте PostgreSQL в .env
───────────────────────────────────────────────────────────────

ssh root@45.141.78.168
cd /opt/asian-goods-store
nano backend/.env

# Убедитесь, что есть настройки PostgreSQL:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=magazin
# PGUSER=postgres
# PGPASSWORD=ваш_пароль

───────────────────────────────────────────────────────────────

ШАГ 4: На сервере - установите зависимости и соберите
───────────────────────────────────────────────────────────────

cd /opt/asian-goods-store
npm install --production=false
cd backend && npm install && cd ..
npm run build

───────────────────────────────────────────────────────────────

ШАГ 5: На сервере - проверьте PostgreSQL и запустите PM2
───────────────────────────────────────────────────────────────

source backend/.env
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT 1;"

pm2 delete all 2>/dev/null || true
pm2 start ecosystem.config.js
pm2 save
pm2 startup
pm2 status

───────────────────────────────────────────────────────────────

ШАГ 6: Проверка работы
───────────────────────────────────────────────────────────────

curl http://45.141.78.168:3001/api/health

# Или в браузере:
# http://45.141.78.168:3000

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
ОБНОВЛЕНИЕ ПРОЕКТА
═══════════════════════════════════════════════════════════════

Автоматически:
.\deploy-to-server-pm2.ps1
Выберите: 2 (Только загрузить код), затем 3 (Обновить и перезапустить)

Вручную:
# 1. Загрузите код (ШАГ 2 выше)
# 2. На сервере:
ssh root@45.141.78.168 "cd /opt/asian-goods-store && npm install && cd backend && npm install && cd .. && npm run build && pm2 restart all"

═══════════════════════════════════════════════════════════════
ПРОВЕРКА СТАТУСА
═══════════════════════════════════════════════════════════════

# Статус PM2
ssh root@45.141.78.168 "cd /opt/asian-goods-store && pm2 status"

# Логи
ssh root@45.141.78.168 "cd /opt/asian-goods-store && pm2 logs --lines 50"

# API Health
curl http://45.141.78.168:3001/api/health

═══════════════════════════════════════════════════════════════
BACKUP POSTGRESQL
═══════════════════════════════════════════════════════════════

Автоматически:
.\deploy-to-server-pm2.ps1
Выберите: 7 (Создать backup базы данных)

Вручную:
ssh root@45.141.78.168 "cd /opt/asian-goods-store && source backend/.env && pg_dump -h \$PGHOST -U \$PGUSER -d \$PGDATABASE > backup_\$(date +%Y%m%d_%H%M%S).sql"

═══════════════════════════════════════════════════════════════

