═══════════════════════════════════════════════════════════════
⚡ БЫСТРОЕ ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С ПАРОЛЕМ В PM2
═══════════════════════════════════════════════════════════════

Проблема: PM2 не загружает переменные из backend/.env
Решение: Исправить путь в server.js + добавить POSTGRES_URL

═══════════════════════════════════════════════════════════════
ШАГ 1: ОБНОВИТЬ КОД НА СЕРВЕРЕ
═══════════════════════════════════════════════════════════════

# Обновите проект из Git (если изменения уже закоммичены):
cd /opt/asian-goods-store
git pull origin main

# ИЛИ исправьте вручную:
nano backend/server.js

# Найдите строку:
# require('dotenv').config({ path: path.resolve(__dirname, '..', '.env') });

# Замените на:
# require('dotenv').config({ path: path.resolve(__dirname, '.env') });

# И добавьте после неё:
# try {
#   require('dotenv').config({ path: path.resolve(__dirname, '..', '.env') });
# } catch (_) {}

═══════════════════════════════════════════════════════════════
ШАГ 2: ДОБАВИТЬ POSTGRES_URL В .env (АЛЬТЕРНАТИВА)
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
echo "POSTGRES_URL=postgres://magazin_user:27061406@localhost:5432/magazin" >> backend/.env

# Проверка:
tail -1 backend/.env

═══════════════════════════════════════════════════════════════
ШАГ 3: ПЕРЕЗАПУСТИТЬ PM2
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
pm2 delete all
pm2 start ecosystem.config.js
pm2 save
sleep 3
pm2 logs asian-goods-backend --lines 30

# Должно быть:
# ✅ Подключение к PostgreSQL установлено
# ✅ Таблицы PostgreSQL созданы/обновлены
# 🚀 Сервер запущен на порту 3001

═══════════════════════════════════════════════════════════════
ВСЕ В ОДНОЙ КОМАНДЕ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store && \
git pull origin main && \
echo "POSTGRES_URL=postgres://magazin_user:27061406@localhost:5432/magazin" >> backend/.env && \
pm2 delete all && \
pm2 start ecosystem.config.js && \
pm2 save && \
sleep 3 && \
pm2 logs asian-goods-backend --lines 20

═══════════════════════════════════════════════════════════════
ПРОВЕРКА РАБОТЫ
═══════════════════════════════════════════════════════════════

# Проверка API
curl http://localhost:3001/api/health

# Должен вернуть: {"status":"OK",...}

# Проверка в браузере:
# http://45.141.78.168:3000
# http://45.141.78.168:3001/api/health

═══════════════════════════════════════════════════════════════

