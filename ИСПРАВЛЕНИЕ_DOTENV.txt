═══════════════════════════════════════════════════════════════
🔧 ИСПРАВЛЕНИЕ ЗАГРУЗКИ .env В PM2
═══════════════════════════════════════════════════════════════

Проблема: PM2 не загружает переменные из backend/.env
Причина: server.js ищет .env в корне, а файл в backend/.env

═══════════════════════════════════════════════════════════════
РЕШЕНИЕ 1: ИСПРАВИТЬ server.js (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════

На сервере выполните:

cd /opt/asian-goods-store
nano backend/server.js

# Найдите строки (в начале файла):
# const path = require('path');
# require('dotenv').config({ path: path.resolve(__dirname, '..', '.env') });

# Замените на:
# require('dotenv').config({ path: path.resolve(__dirname, '.env') });

# ИЛИ добавьте загрузку из обоих мест:
const path = require('path');
// Загружаем переменные окружения из backend/.env
try {
  require('dotenv').config({ path: path.resolve(__dirname, '.env') });
} catch (_) {}
// Также пробуем загрузить из корня (для совместимости)
try {
  require('dotenv').config({ path: path.resolve(__dirname, '..', '.env') });
} catch (_) {}

# Сохраните: Ctrl+O, Enter, Ctrl+X

═══════════════════════════════════════════════════════════════
РЕШЕНИЕ 2: ДОБАВИТЬ ПЕРЕМЕННЫЕ В ecosystem.config.js
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
nano ecosystem.config.js

# В секции env для asian-goods-backend добавьте:
env: {
  NODE_ENV: 'production',
  PORT: 3001,
  PGHOST: 'localhost',
  PGPORT: '5432',
  PGDATABASE: 'magazin',
  PGUSER: 'magazin_user',
  PGPASSWORD: '27061406',
  FRONTEND_URL: 'https://45.141.78.168',
  JWT_SECRET: 'cf27ac8560bcc7127456d5c8c33a6f0433f5d6263e1af098a7aa6a2cb422f49b'
}

# Сохраните: Ctrl+O, Enter, Ctrl+X

═══════════════════════════════════════════════════════════════
РЕШЕНИЕ 3: ИСПОЛЬЗОВАТЬ POSTGRES_URL
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
nano backend/.env

# Добавьте в конец файла:
POSTGRES_URL=postgres://magazin_user:27061406@localhost:5432/magazin

# Сохраните: Ctrl+O, Enter, Ctrl+X

# Это будет работать, так как postgres.js проверяет POSTGRES_URL первым

═══════════════════════════════════════════════════════════════
ПОСЛЕ ИСПРАВЛЕНИЯ
═══════════════════════════════════════════════════════════════

# Перезапустите PM2
cd /opt/asian-goods-store
pm2 delete all
pm2 start ecosystem.config.js
pm2 save

# Проверьте логи
pm2 logs asian-goods-backend --lines 30

# Должно быть:
# ✅ Подключение к PostgreSQL установлено
# ✅ Таблицы PostgreSQL созданы/обновлены
# 🚀 Сервер запущен на порту 3001

═══════════════════════════════════════════════════════════════
БЫСТРОЕ ИСПРАВЛЕНИЕ (РЕКОМЕНДУЕТСЯ)
═══════════════════════════════════════════════════════════════

# 1. Исправить server.js
cd /opt/asian-goods-store
sed -i "s|path.resolve(__dirname, '..', '.env')|path.resolve(__dirname, '.env')|g" backend/server.js

# 2. Добавить POSTGRES_URL в .env (на всякий случай)
echo "POSTGRES_URL=postgres://magazin_user:27061406@localhost:5432/magazin" >> backend/.env

# 3. Перезапустить
pm2 delete all
pm2 start ecosystem.config.js
pm2 save
sleep 3
pm2 logs asian-goods-backend --lines 20

═══════════════════════════════════════════════════════════════

