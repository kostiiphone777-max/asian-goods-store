# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА СЕРВЕР

**Сервер:** root@45.141.78.168  
**База данных:** PostgreSQL (нужно создать БД и пользователя)  
**Менеджер процессов:** PM2

---

## 📋 ОГЛАВЛЕНИЕ

1. [Подготовка сервера](#1-подготовка-сервера)
2. [Создание пользователя и базы данных PostgreSQL](#2-создание-пользователя-и-базы-данных-postgresql)
3. [Загрузка проекта на сервер](#3-загрузка-проекта-на-сервер)
4. [Настройка переменных окружения](#4-настройка-переменных-окружения)
5. [Установка зависимостей](#5-установка-зависимостей)
6. [Создание схемы базы данных](#6-создание-схемы-базы-данных)
7. [Сборка frontend](#7-сборка-frontend)
8. [Создание администратора](#8-создание-администратора)
9. [Запуск через PM2](#9-запуск-через-pm2)
10. [Проверка работы](#10-проверка-работы)
11. [Настройка автозапуска](#11-настройка-автозапуска)
12. [Полезные команды](#12-полезные-команды)

---

## 1. ПОДГОТОВКА СЕРВЕРА

### 1.1 Подключение к серверу

```bash
ssh root@45.141.78.168
```

### 1.2 Проверка установленных инструментов

```bash
# Проверка Node.js
node --version

# Проверка npm
npm --version

# Проверка PM2
pm2 --version

# Проверка PostgreSQL
psql --version
```

### 1.3 Установка Node.js (если не установлен)

```bash
# Для Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs

# Проверка
node --version
npm --version
```

### 1.4 Установка PM2 (если не установлен)

```bash
npm install -g pm2

# Проверка
pm2 --version
```

### 1.5 Проверка статуса PostgreSQL

```bash
# Проверка, запущен ли PostgreSQL
systemctl status postgresql

# Если не запущен, запустите:
systemctl start postgresql
systemctl enable postgresql
```

---

## 2. СОЗДАНИЕ ПОЛЬЗОВАТЕЛЯ И БАЗЫ ДАННЫХ POSTGRESQL

### 2.1 Подключение к PostgreSQL

```bash
# Переключитесь на пользователя postgres
sudo -u postgres psql

# Или напрямую:
psql -U postgres
```

### 2.2 Создание пользователя и базы данных

**Внутри psql выполните:**

```sql
-- Создание пользователя (если еще не существует)
CREATE USER magazin_user WITH PASSWORD 'ваш_надежный_пароль';

-- Создание базы данных
CREATE DATABASE magazin OWNER magazin_user;

-- Выдача всех привилегий пользователю на базу данных
GRANT ALL PRIVILEGES ON DATABASE magazin TO magazin_user;

-- Выход из psql
\q
```

**⚠️ ВАЖНО:** Замените `ваш_надежный_пароль` на надежный пароль! Запомните его, он понадобится в `.env` файле.

### 2.3 Проверка подключения

**⚠️ ВАЖНО:** PostgreSQL использует peer authentication для локальных подключений через Unix socket. Для проверки подключения **ОБЯЗАТЕЛЬНО** используйте `-h localhost`, чтобы принудительно использовать TCP/IP подключение с паролем.

```bash
# Вариант 1: С вводом пароля (введите пароль при запросе)
psql -h localhost -U magazin_user -d magazin -c "SELECT version();"
# Пароль: ваш_надежный_пароль

# Вариант 2: С переменной окружения (без ввода пароля)
PGPASSWORD=ваш_надежный_пароль psql -h localhost -U magazin_user -d magazin -c "SELECT version();"
```

**Если видите версию PostgreSQL - подключение работает! ✅**

**Если возникает ошибка "Peer authentication failed":**

Это нормально! PostgreSQL по умолчанию использует peer authentication для локальных подключений. Решение - всегда используйте `-h localhost` для принудительного TCP/IP подключения:

```bash
# ❌ НЕПРАВИЛЬНО (будет ошибка peer authentication):
psql -U magazin_user -d magazin

# ✅ ПРАВИЛЬНО (использует пароль):
psql -h localhost -U magazin_user -d magazin
```

### 2.4 Альтернативный вариант: использование пользователя postgres

Если вы хотите использовать стандартного пользователя `postgres`:

```bash
# Установите пароль для пользователя postgres
sudo -u postgres psql
ALTER USER postgres WITH PASSWORD 'ваш_пароль';
\q

# Создайте базу данных
sudo -u postgres createdb magazin
```

---

## 3. ЗАГРУЗКА ПРОЕКТА НА СЕРВЕР

### 3.1 Создание директории проекта

```bash
mkdir -p /opt/asian-goods-store
cd /opt/asian-goods-store
```

### 3.2 Вариант A: Загрузка через Git (если проект в репозитории)

```bash
# Клонирование репозитория
git clone ВАШ_РЕПОЗИТОРИЙ .

# Или если репозиторий уже существует:
cd /opt/asian-goods-store
git pull origin main
```

### 3.2 Вариант B: Загрузка через SCP (с вашего компьютера)

**На вашем компьютере (Windows PowerShell):**

```powershell
# Перейдите в директорию проекта
cd "C:\Работа\ПРОЕКТЫ\Азиатские товары. Интернет магазин\asian-goods-store"

# Создайте архив (исключая ненужные файлы)
tar -czf deploy.tar.gz `
    --exclude=node_modules `
    --exclude=.next `
    --exclude=backend/node_modules `
    --exclude=backend/uploads/* `
    --exclude=backend/database/store.db `
    --exclude=backend/database/*.db `
    --exclude=.git `
    --exclude=logs `
    --exclude=*.log `
    *

# Загрузите на сервер
scp deploy.tar.gz root@45.141.78.168:/opt/asian-goods-store/

# На сервере распакуйте
ssh root@45.141.78.168 "cd /opt/asian-goods-store && tar -xzf deploy.tar.gz && rm deploy.tar.gz"
```

### 3.3 Вариант C: Использование готового скрипта (Windows)

**На вашем компьютере:**

```powershell
.\deploy-to-server-pm2.ps1
```

Выберите пункт **1 (Полное развертывание)**

---

## 4. НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

### 4.1 Создание файла .env для backend

```bash
cd /opt/asian-goods-store
nano backend/.env
```

**Скопируйте и вставьте следующее содержимое:**

```env
# Порт сервера
PORT=3001

# URL фронтенда для CORS
FRONTEND_URL=http://45.141.78.168:3000

# Секретный ключ для JWT токенов (ЗАМЕНИТЕ НА СЛУЧАЙНУЮ СТРОКУ!)
JWT_SECRET=ваш_секретный_ключ_минимум_32_символа_случайные_буквы_и_цифры

# Режим работы
NODE_ENV=production

# PostgreSQL настройки
DB_CLIENT=postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=magazin
PGUSER=magazin_user
PGPASSWORD=ваш_надежный_пароль
```

**⚠️ ВАЖНО:**
- Замените `JWT_SECRET` на случайную строку минимум 32 символа
- Замените `PGPASSWORD` на пароль, который вы установили для пользователя PostgreSQL
- Если используете пользователя `postgres`, замените `PGUSER=magazin_user` на `PGUSER=postgres`

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### 4.2 Создание файла .env.local для frontend

```bash
nano .env.local
```

**Скопируйте и вставьте:**

```env
NEXT_PUBLIC_API_URL=http://45.141.78.168:3001/api
```

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

---

## 5. УСТАНОВКА ЗАВИСИМОСТЕЙ

### 5.1 Установка зависимостей frontend

```bash
cd /opt/asian-goods-store
npm install --production=false
```

### 5.2 Установка зависимостей backend

```bash
cd /opt/asian-goods-store/backend
npm install
cd ..
```

---

## 6. СОЗДАНИЕ СХЕМЫ БАЗЫ ДАННЫХ

### 6.1 Применение схемы PostgreSQL

```bash
cd /opt/asian-goods-store

# Загрузите переменные окружения
source backend/.env

# Проверьте, что переменные загружены
echo "Host: $PGHOST"
echo "User: $PGUSER"
echo "Database: $PGDATABASE"

# Примените схему
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -f backend/database/schema.postgres.sql
```

**Если переменные не загружаются, используйте прямые значения:**

```bash
psql -h localhost -U magazin_user -d magazin -f backend/database/schema.postgres.sql
```

**Или если используете пользователя postgres:**

```bash
psql -h localhost -U postgres -d magazin -f backend/database/schema.postgres.sql
```

### 6.2 Проверка создания таблиц

```bash
psql -h localhost -U magazin_user -d magazin -c "\dt"
```

Должны быть созданы таблицы:
- users
- categories
- products
- orders
- order_items
- cart_items
- telegram_settings

---

## 7. СБОРКА FRONTEND

```bash
cd /opt/asian-goods-store
npm run build
```

Это может занять несколько минут. Дождитесь завершения.

---

## 8. СОЗДАНИЕ АДМИНИСТРАТОРА

### 8.1 Создание директории для логов

```bash
mkdir -p /opt/asian-goods-store/logs
```

### 8.2 Создание администратора

```bash
cd /opt/asian-goods-store/backend
source ../backend/.env 2>/dev/null || true
node scripts/create-admin-user.js
```

**Результат:** Будет создан администратор:
- **Email:** admin@example.com
- **Пароль:** admin123

**⚠️ ВАЖНО:** После первого входа смените пароль администратора!

---

## 9. ЗАПУСК ЧЕРЕЗ PM2

### 9.1 Остановка старых процессов (если есть)

```bash
cd /opt/asian-goods-store
pm2 delete all 2>/dev/null || true
```

### 9.2 Запуск приложений

```bash
pm2 start ecosystem.config.js
```

### 9.3 Проверка статуса

```bash
pm2 status
```

Должны быть запущены два процесса:
- `asian-goods-backend` (порт 3001)
- `asian-goods-frontend` (порт 3000)

### 9.4 Просмотр логов

```bash
# Все логи
pm2 logs

# Логи backend
pm2 logs asian-goods-backend --lines 50

# Логи frontend
pm2 logs asian-goods-frontend --lines 50

# Только ошибки
pm2 logs --err
```

---

## 10. ПРОВЕРКА РАБОТЫ

### 10.1 Проверка API

```bash
# Health check
curl http://localhost:3001/api/health

# Тестовый endpoint
curl http://localhost:3001/api/test
```

### 10.2 Проверка через браузер

Откройте в браузере:
- **Frontend:** http://45.141.78.168:3000
- **Backend API:** http://45.141.78.168:3001/api/health
- **Админ-панель:** http://45.141.78.168:3000/admin

### 10.3 Проверка подключения к базе данных

```bash
cd /opt/asian-goods-store/backend
source ../backend/.env

# Используйте переменную окружения для пароля
PGPASSWORD=$PGPASSWORD psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT COUNT(*) FROM users;"

# ИЛИ если переменные не работают:
PGPASSWORD=ваш_пароль psql -h localhost -U magazin_user -d magazin -c "SELECT COUNT(*) FROM users;"
```

---

## 11. НАСТРОЙКА АВТОЗАПУСКА

### 11.1 Сохранение конфигурации PM2

```bash
pm2 save
```

### 11.2 Настройка автозапуска при перезагрузке сервера

```bash
pm2 startup
```

**Выполните команду, которую PM2 выведет.** Обычно это что-то вроде:

```bash
sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```

### 11.3 Проверка автозапуска

```bash
# Перезагрузите сервер (опционально, для проверки)
# reboot

# После перезагрузки проверьте:
pm2 status
```

---

## 12. ПОЛЕЗНЫЕ КОМАНДЫ

### 12.1 Управление PM2

```bash
# Статус всех процессов
pm2 status

# Перезапуск всех процессов
pm2 restart all

# Перезапуск конкретного процесса
pm2 restart asian-goods-backend

# Остановка всех процессов
pm2 stop all

# Удаление всех процессов
pm2 delete all

# Просмотр логов в реальном времени
pm2 logs

# Мониторинг ресурсов
pm2 monit
```

### 12.2 Работа с базой данных

```bash
# Подключение к базе данных
psql -h localhost -U magazin_user -d magazin

# Создание backup
pg_dump -h localhost -U magazin_user -d magazin > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановление из backup
psql -h localhost -U magazin_user -d magazin < backup_YYYYMMDD_HHMMSS.sql
```

### 12.3 Обновление проекта

```bash
cd /opt/asian-goods-store

# Если используете Git:
git pull origin main

# Установка новых зависимостей
npm install --production=false
cd backend && npm install && cd ..

# Пересборка frontend
npm run build

# Перезапуск PM2
pm2 restart all
```

### 12.4 Проверка портов

```bash
# Проверка, какие порты заняты
netstat -tulpn | grep -E ':(3000|3001)'

# Или
ss -tulpn | grep -E ':(3000|3001)'
```

### 12.5 Проверка логов системы

```bash
# Логи PostgreSQL
tail -f /var/log/postgresql/postgresql-*.log

# Логи системы
journalctl -u postgresql -f
```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Backend не запускается

```bash
# Проверьте логи
pm2 logs asian-goods-backend --err --lines 50

# Проверьте подключение к базе данных
cd /opt/asian-goods-store/backend
source ../backend/.env
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT 1;"

# Проверьте переменные окружения
cat backend/.env
```

### Проблема: Ошибка подключения к PostgreSQL

**Ошибка "Peer authentication failed":**

Это нормально! PostgreSQL использует peer authentication для локальных подключений. **Всегда используйте `-h localhost`** для принудительного TCP/IP подключения:

```bash
# ❌ НЕПРАВИЛЬНО (будет ошибка):
psql -U magazin_user -d magazin

# ✅ ПРАВИЛЬНО (использует пароль):
psql -h localhost -U magazin_user -d magazin

# ИЛИ с переменной окружения:
PGPASSWORD=ваш_пароль psql -h localhost -U magazin_user -d magazin
```

**Другие проверки:**

```bash
# Проверьте, запущен ли PostgreSQL
systemctl status postgresql

# Проверьте настройки аутентификации
cat /etc/postgresql/*/main/pg_hba.conf | grep -v "^#"

# Перезапустите PostgreSQL
systemctl restart postgresql
```

### Проблема: Frontend не собирается

```bash
# Очистите кэш
rm -rf .next
npm run build
```

### Проблема: Порт уже занят

```bash
# Найдите процесс, использующий порт
lsof -i :3000
lsof -i :3001

# Убейте процесс (замените PID на реальный)
kill -9 PID
```

---

## 📝 ЧЕКЛИСТ ДЕПЛОЯ

- [ ] Подключились к серверу
- [ ] Установили Node.js, npm, PM2
- [ ] Проверили статус PostgreSQL
- [ ] Создали пользователя PostgreSQL
- [ ] Создали базу данных `magazin`
- [ ] Загрузили проект на сервер
- [ ] Создали `backend/.env` с правильными настройками
- [ ] Создали `.env.local` для frontend
- [ ] Установили зависимости frontend и backend
- [ ] Применили схему базы данных
- [ ] Собрали frontend (`npm run build`)
- [ ] Создали администратора
- [ ] Запустили через PM2
- [ ] Проверили работу API
- [ ] Настроили автозапуск PM2
- [ ] Проверили доступность сайта в браузере

---

## 🎉 ГОТОВО!

Ваш проект должен быть доступен по адресу:
- **Сайт:** http://45.141.78.168:3000
- **API:** http://45.141.78.168:3001/api
- **Админ-панель:** http://45.141.78.168:3000/admin

**Данные для входа в админ-панель:**
- Email: `admin@example.com`
- Пароль: `admin123`

**⚠️ ВАЖНО:** Смените пароль администратора после первого входа!

---

## 📞 ДОПОЛНИТЕЛЬНАЯ ПОМОЩЬ

Если возникли проблемы:
1. Проверьте логи: `pm2 logs`
2. Проверьте подключение к БД: `psql -h localhost -U magazin_user -d magazin`
3. Проверьте переменные окружения: `cat backend/.env`
4. Проверьте статус процессов: `pm2 status`

---

**Успешного деплоя! 🚀**

