═══════════════════════════════════════════════════════════════
🔧 ВОССТАНОВЛЕНИЕ PM2 ПРОЦЕССОВ
═══════════════════════════════════════════════════════════════

Проблема: PM2 процессы не запущены, отсутствует директория logs

═══════════════════════════════════════════════════════════════
ШАГ 1: СОЗДАНИЕ ДИРЕКТОРИИ ДЛЯ ЛОГОВ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
mkdir -p logs

# Проверка
ls -la logs

═══════════════════════════════════════════════════════════════
ШАГ 2: ПРОВЕРКА ecosystem.config.js
═══════════════════════════════════════════════════════════════

# Убедитесь, что файл существует
cat ecosystem.config.js

# Если файла нет, создайте его (см. ниже)

═══════════════════════════════════════════════════════════════
ШАГ 3: ЗАПУСК ПРОЦЕССОВ PM2
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Удаление старых процессов (если есть)
pm2 delete all 2>/dev/null || true

# Запуск процессов
pm2 start ecosystem.config.js

# Сохранение конфигурации
pm2 save

# Проверка статуса
pm2 status

# Должны быть запущены 2 процесса:
# - asian-goods-backend
# - asian-goods-frontend

═══════════════════════════════════════════════════════════════
ШАГ 4: ПРОВЕРКА ЛОГОВ
═══════════════════════════════════════════════════════════════

# Логи backend
pm2 logs asian-goods-backend --lines 30

# Логи frontend
pm2 logs asian-goods-frontend --lines 30

# Все логи
pm2 logs --lines 20

═══════════════════════════════════════════════════════════════
ШАГ 5: ПРОВЕРКА РАБОТЫ
═══════════════════════════════════════════════════════════════

# Проверка API
curl http://localhost:3001/api/health

# Должен вернуть: {"status":"OK",...}

# Проверка в браузере:
# http://45.141.78.168:3000
# http://45.141.78.168:3001/api/health

═══════════════════════════════════════════════════════════════
ЕСЛИ ecosystem.config.js ОТСУТСТВУЕТ
═══════════════════════════════════════════════════════════════

Создайте файл:

cd /opt/asian-goods-store
nano ecosystem.config.js

# Вставьте:
module.exports = {
  apps: [
    {
      name: 'asian-goods-backend',
      script: './backend/server.js',
      cwd: './',
      instances: 1,
      exec_mode: 'fork',
      watch: false,
      max_memory_restart: '500M',
      env: {
        NODE_ENV: 'production',
        PORT: 3001
      },
      error_file: './logs/backend-error.log',
      out_file: './logs/backend-out.log',
      log_file: './logs/backend-combined.log',
      time: true,
      merge_logs: true,
      autorestart: true,
      max_restarts: 10,
      min_uptime: '10s'
    },
    {
      name: 'asian-goods-frontend',
      script: 'node_modules/next/dist/bin/next',
      args: 'start -p 3000',
      cwd: './',
      instances: 1,
      exec_mode: 'fork',
      watch: false,
      max_memory_restart: '1G',
      env: {
        NODE_ENV: 'production',
        PORT: 3000
      },
      error_file: './logs/frontend-error.log',
      out_file: './logs/frontend-out.log',
      log_file: './logs/frontend-combined.log',
      time: true,
      merge_logs: true,
      autorestart: true,
      max_restarts: 10,
      min_uptime: '10s'
    }
  ]
};

# Сохраните: Ctrl+O, Enter, Ctrl+X

═══════════════════════════════════════════════════════════════
ВСЕ В ОДНОЙ КОМАНДЕ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store && \
mkdir -p logs && \
pm2 delete all 2>/dev/null || true && \
pm2 start ecosystem.config.js && \
pm2 save && \
pm2 status

═══════════════════════════════════════════════════════════════
ЕСЛИ ПРОЦЕССЫ НЕ ЗАПУСКАЮТСЯ
═══════════════════════════════════════════════════════════════

# Проверьте, что .env файлы на месте
cat backend/.env

# Проверьте, что зависимости установлены
ls -la node_modules | head -5
ls -la backend/node_modules | head -5

# Проверьте, что frontend собран
ls -la .next

# Если .next отсутствует, соберите:
npm run build

# Запустите процессы вручную для проверки:
cd backend
node server.js
# (Ctrl+C для остановки)

# Если backend запускается, значит проблема в PM2 конфигурации

═══════════════════════════════════════════════════════════════

