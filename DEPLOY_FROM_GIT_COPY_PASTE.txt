═══════════════════════════════════════════════════════════════
🚀 ПЕРЕНОС НА СЕРВЕР ИЗ GIT (COPY & PASTE)
═══════════════════════════════════════════════════════════════

Сервер: 45.141.78.168
База данных: PostgreSQL

═══════════════════════════════════════════════════════════════
ШАГ 1: Подключитесь к серверу
═══════════════════════════════════════════════════════════════

ssh root@45.141.78.168

───────────────────────────────────────────────────────────────

ШАГ 2: Перейдите в директорию проекта
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

───────────────────────────────────────────────────────────────

ШАГ 3: Обновите код из Git
═══════════════════════════════════════════════════════════════

git pull origin main

───────────────────────────────────────────────────────────────

ШАГ 4: Удалите старые SQLite файлы (если есть)
═══════════════════════════════════════════════════════════════

rm -f backend/database/store.db
rm -f backend/database/schema.sql
rm -f backend/scripts/init-database.js
rm -f backend/scripts/migrate-to-sqlite.js

───────────────────────────────────────────────────────────────

ШАГ 5: Установите зависимости
═══════════════════════════════════════════════════════════════

npm install --production=false

cd backend && npm install && cd ..

───────────────────────────────────────────────────────────────

ШАГ 6: Соберите frontend
═══════════════════════════════════════════════════════════════

npm run build

───────────────────────────────────────────────────────────────

ШАГ 7: Проверьте настройки PostgreSQL в .env
═══════════════════════════════════════════════════════════════

cat backend/.env | grep -E 'PGHOST|PGPORT|PGDATABASE|PGUSER|PGPASSWORD'

# Если настройки отсутствуют, отредактируйте:
nano backend/.env

# Добавьте (если нет):
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=magazin
# PGUSER=postgres
# PGPASSWORD=ваш_пароль

───────────────────────────────────────────────────────────────

ШАГ 8: Проверьте подключение к PostgreSQL
═══════════════════════════════════════════════════════════════

source backend/.env
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT 1;"

# Если ошибка - проверьте, запущен ли PostgreSQL:
systemctl status postgresql
systemctl start postgresql  # если не запущен

───────────────────────────────────────────────────────────────

ШАГ 9: Примените схему PostgreSQL (если таблиц нет)
═══════════════════════════════════════════════════════════════

psql -h $PGHOST -U $PGUSER -d $PGDATABASE -f backend/database/schema.postgres.sql

# Проверьте наличие таблиц:
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "\dt"

───────────────────────────────────────────────────────────────

ШАГ 10: Перезапустите PM2
═══════════════════════════════════════════════════════════════

pm2 restart all

# Или если нужно перезапустить только backend:
pm2 restart asian-goods-backend

───────────────────────────────────────────────────────────────

ШАГ 11: Проверьте статус
═══════════════════════════════════════════════════════════════

pm2 status

# Backend должен быть "online" (зеленый)

───────────────────────────────────────────────────────────────

ШАГ 12: Проверьте логи
═══════════════════════════════════════════════════════════════

pm2 logs asian-goods-backend --lines 20

# Не должно быть ошибок про schema.sql
# Должно быть: "✅ Подключение к PostgreSQL установлено"

───────────────────────────────────────────────────────────────

ШАГ 13: Проверьте работу API
═══════════════════════════════════════════════════════════════

curl http://localhost:3001/api/health

# Должен вернуть: {"status":"OK",...}

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
ВСЕ В ОДНОЙ КОМАНДЕ (для опытных)
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store && \
git pull origin main && \
rm -f backend/database/store.db backend/database/schema.sql && \
npm install --production=false && \
cd backend && npm install && cd .. && \
npm run build && \
pm2 restart all && \
pm2 status

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
ЕСЛИ BACKEND ПАДАЕТ (ошибка schema.sql)
═══════════════════════════════════════════════════════════════

# Убедитесь, что используется правильный database.js
cat backend/database/database.js | head -10

# Должно быть:
# const pg = require('./postgres');
# ...
# async init() {
#   return pg.init();
# }

# Если нет - загрузите правильный файл с вашего компьютера:
# scp backend/database/database.js root@45.141.78.168:/opt/asian-goods-store/backend/database/database.js

# Также проверьте наличие файлов:
ls -la backend/database/postgres.js
ls -la backend/database/schema.postgres.sql

# Если их нет - загрузите с вашего компьютера:
# scp backend/database/postgres.js root@45.141.78.168:/opt/asian-goods-store/backend/database/postgres.js
# scp backend/database/schema.postgres.sql root@45.141.78.168:/opt/asian-goods-store/backend/database/schema.postgres.sql

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
МИГРАЦИЯ ДАННЫХ ИЗ SQLITE (если нужно)
═══════════════════════════════════════════════════════════════

# Если у вас есть старая SQLite база с данными:

# 1. Загрузите store.db на сервер (если его нет)
# scp backend/database/store.db root@45.141.78.168:/opt/asian-goods-store/backend/database/store.db

# 2. Выполните миграцию:
cd /opt/asian-goods-store/backend
node scripts/migrate-sqlite-to-postgres.js

# 3. Проверьте данные:
source ../backend/.env
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT COUNT(*) FROM users;"
psql -h $PGHOST -U $PGUSER -d $PGDATABASE -c "SELECT COUNT(*) FROM products;"

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════

