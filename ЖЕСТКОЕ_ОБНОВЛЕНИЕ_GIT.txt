═══════════════════════════════════════════════════════════════
🔄 ЖЕСТКОЕ ОБНОВЛЕНИЕ ИЗ GIT (отбросить локальные изменения)
═══════════════════════════════════════════════════════════════

ВНИМАНИЕ: Это удалит все локальные изменения и обновит проект 
до последней версии из репозитория.

═══════════════════════════════════════════════════════════════
ШАГ 1: СОХРАНЕНИЕ ВАЖНЫХ ФАЙЛОВ (если нужно)
═══════════════════════════════════════════════════════════════

# Сохраните .env файлы (если они были изменены):
cd /opt/asian-goods-store

# Создайте резервную копию .env
cp backend/.env backend/.env.backup 2>/dev/null || true
cp .env.local .env.local.backup 2>/dev/null || true

═══════════════════════════════════════════════════════════════
ШАГ 2: ОТБРОС ЛОКАЛЬНЫХ ИЗМЕНЕНИЙ И ОБНОВЛЕНИЕ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Отбросить все локальные изменения
git reset --hard HEAD

# Очистить неотслеживаемые файлы (опционально, будьте осторожны!)
git clean -fd

# Обновить из репозитория
git pull origin main

═══════════════════════════════════════════════════════════════
ШАГ 3: ВОССТАНОВЛЕНИЕ .env ФАЙЛОВ
═══════════════════════════════════════════════════════════════

# Если нужно восстановить .env из резервной копии:
# cp backend/.env.backup backend/.env

# ИЛИ создайте заново:
nano backend/.env

# Вставьте:
PORT=3001
FRONTEND_URL=https://45.141.78.168
JWT_SECRET=cf27ac8560bcc7127456d5c8c33a6f0433f5d6263e1af098a7aa6a2cb422f49b
NODE_ENV=production
DB_CLIENT=postgres
PGHOST=localhost
PGPORT=5432
PGDATABASE=magazin
PGUSER=magazin_user
PGPASSWORD=27061406

# Сохраните: Ctrl+O, Enter, Ctrl+X

# .env.local для frontend:
nano .env.local

# Вставьте:
NEXT_PUBLIC_API_URL=http://45.141.78.168:3001/api

# Сохраните: Ctrl+O, Enter, Ctrl+X

═══════════════════════════════════════════════════════════════
ШАГ 4: УСТАНОВКА ЗАВИСИМОСТЕЙ И ПЕРЕСБОРКА
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Установка зависимостей
npm install --production=false
cd backend && npm install && cd ..

# Пересборка frontend
npm run build

═══════════════════════════════════════════════════════════════
ШАГ 5: ПЕРЕЗАПУСК PM2
═══════════════════════════════════════════════════════════════

pm2 restart all

# Проверка статуса
pm2 status

# Проверка логов
pm2 logs --lines 20

═══════════════════════════════════════════════════════════════
ВСЕ В ОДНОЙ КОМАНДЕ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store && \
cp backend/.env backend/.env.backup 2>/dev/null || true && \
cp .env.local .env.local.backup 2>/dev/null || true && \
git reset --hard HEAD && \
git clean -fd && \
git pull origin main && \
npm install --production=false && \
cd backend && npm install && cd .. && \
npm run build && \
pm2 restart all && \
pm2 status

# После этого восстановите .env файлы вручную (см. ШАГ 3)

═══════════════════════════════════════════════════════════════

