# 🛍️ Asian Goods Store - Backend API

Backend API для интернет-магазина азиатских товаров, построенный на Node.js с файловой базой данных.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
cd backend
npm install
```

### Инициализация базы данных
```bash
npm run init-db
```

### Запуск сервера
```bash
# Режим разработки
npm run dev

# Продакшн
npm start
```

Сервер будет доступен по адресу: `http://localhost:3001`

## 📊 API Endpoints

### Общие
- `GET /api/health` - Проверка состояния сервера

### Товары
- `GET /api/products` - Получить все товары (с фильтрацией и поиском)
- `GET /api/products/:id` - Получить товар по ID
- `GET /api/products/slug/:slug` - Получить товар по slug
- `GET /api/products/category/:categoryId` - Получить товары по категории
- `POST /api/products` - Создать товар (требует авторизации)
- `PUT /api/products/:id` - Обновить товар (требует авторизации)
- `DELETE /api/products/:id` - Удалить товар (требует авторизации)
- `GET /api/products/:id/similar` - Получить похожие товары

### Категории
- `GET /api/categories` - Получить все категории
- `GET /api/categories?tree=true` - Получить категории в виде дерева
- `GET /api/categories/root` - Получить корневые категории
- `GET /api/categories/:id` - Получить категорию по ID
- `GET /api/categories/slug/:slug` - Получить категорию по slug
- `GET /api/categories/:id/children` - Получить подкатегории
- `GET /api/categories/:id/products` - Получить товары категории
- `POST /api/categories` - Создать категорию (требует авторизации)
- `PUT /api/categories/:id` - Обновить категорию (требует авторизации)
- `DELETE /api/categories/:id` - Удалить категорию (требует авторизации)

### Пользователи
- `POST /api/users/register` - Регистрация
- `POST /api/users/login` - Вход
- `GET /api/users/profile` - Получить профиль (требует авторизации)
- `PUT /api/users/profile` - Обновить профиль (требует авторизации)
- `PUT /api/users/change-password` - Изменить пароль (требует авторизации)
- `POST /api/users/addresses` - Добавить адрес (требует авторизации)
- `PUT /api/users/addresses/:addressId` - Обновить адрес (требует авторизации)
- `DELETE /api/users/addresses/:addressId` - Удалить адрес (требует авторизации)

### Корзина
- `GET /api/cart` - Получить корзину
- `POST /api/cart/add` - Добавить товар в корзину
- `PUT /api/cart/update` - Обновить количество товара
- `DELETE /api/cart/remove` - Удалить товар из корзины
- `DELETE /api/cart/clear` - Очистить корзину
- `POST /api/cart/merge` - Объединить корзины

### Заказы
- `POST /api/orders` - Создать заказ (требует авторизации)
- `GET /api/orders/my-orders` - Получить заказы пользователя (требует авторизации)
- `GET /api/orders/:id` - Получить заказ по ID (требует авторизации)
- `GET /api/orders/number/:orderNumber` - Получить заказ по номеру (требует авторизации)
- `PUT /api/orders/:id/status` - Обновить статус заказа (только админ)
- `PUT /api/orders/:id/cancel` - Отменить заказ (требует авторизации)
- `GET /api/orders/admin/stats` - Статистика заказов (только админ)
- `GET /api/orders/admin/all` - Все заказы (только админ)

## 🔐 Авторизация

API использует JWT токены для авторизации. После успешного входа или регистрации, включите токен в заголовок:

```
Authorization: Bearer <your-jwt-token>
```

## 📝 Примеры запросов

### Регистрация пользователя
```bash
curl -X POST http://localhost:3001/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "firstName": "Иван",
    "lastName": "Петров"
  }'
```

### Получение товаров с фильтрацией
```bash
curl "http://localhost:3001/api/products?search=соус&minPrice=500&maxPrice=1000&categoryId=category-id"
```

### Добавление товара в корзину
```bash
curl -X POST http://localhost:3001/api/cart/add \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "productId": "product-id",
    "quantity": 2
  }'
```

### Создание заказа
```bash
curl -X POST http://localhost:3001/api/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "items": [
      {
        "productId": "product-id",
        "quantity": 2,
        "price": 890
      }
    ],
    "shippingAddress": {
      "street": "ул. Примерная, д. 1",
      "city": "Москва",
      "postalCode": "123456",
      "country": "Россия"
    },
    "paymentMethod": "card"
  }'
```

## 🗄️ Структура данных

### Товар (Product)
```json
{
  "id": "uuid",
  "name": "Название товара",
  "description": "Описание",
  "price": 1000,
  "originalPrice": 1200,
  "categoryId": "category-uuid",
  "slug": "product-slug",
  "images": ["/image1.jpg"],
  "badge": "Хит продаж",
  "stock": 50,
  "isActive": true,
  "tags": ["тег1", "тег2"],
  "rating": 4.5,
  "reviewCount": 10,
  "weight": "500g",
  "dimensions": "10x10x10cm",
  "country": "Япония",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

### Категория (Category)
```json
{
  "id": "uuid",
  "name": "Название категории",
  "slug": "category-slug",
  "description": "Описание",
  "image": "/category-image.jpg",
  "parentId": null,
  "isActive": true,
  "sortOrder": 1,
  "metaTitle": "SEO заголовок",
  "metaDescription": "SEO описание",
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

### Пользователь (User)
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "firstName": "Иван",
  "lastName": "Петров",
  "phone": "+7 (999) 123-45-67",
  "role": "customer",
  "isActive": true,
  "emailVerified": true,
  "avatar": "/avatar.jpg",
  "addresses": [
    {
      "id": "uuid",
      "street": "ул. Примерная, д. 1",
      "city": "Москва",
      "postalCode": "123456",
      "country": "Россия",
      "isDefault": true
    }
  ],
  "preferences": {
    "newsletter": true,
    "notifications": true,
    "language": "ru"
  },
  "createdAt": "2024-01-01T00:00:00.000Z",
  "updatedAt": "2024-01-01T00:00:00.000Z"
}
```

## 🔧 Настройка

### Переменные окружения
Создайте файл `.env` в корне backend директории:

```env
PORT=3001
FRONTEND_URL=http://localhost:3000
JWT_SECRET=your-super-secret-jwt-key
NODE_ENV=development
```

### Настройка CORS
По умолчанию CORS настроен для `http://localhost:3000`. Измените `FRONTEND_URL` в переменных окружения для настройки под ваш фронтенд.

## 📁 Структура проекта

```
backend/
├── data/                 # Файловая база данных (JSON)
│   ├── products.json
│   ├── categories.json
│   ├── users.json
│   ├── orders.json
│   └── carts.json
├── models/              # Модели данных
│   ├── Product.js
│   ├── Category.js
│   ├── User.js
│   ├── Order.js
│   └── Cart.js
├── routes/              # API маршруты
│   ├── products.js
│   ├── categories.js
│   ├── users.js
│   ├── orders.js
│   └── cart.js
├── scripts/             # Скрипты
│   └── init-database.js
├── uploads/             # Загруженные файлы
├── server.js            # Основной файл сервера
├── package.json
└── README.md
```

## 🛠️ Разработка

### Добавление новых полей
1. Обновите модель в соответствующем файле в `models/`
2. Обновите валидацию в маршрутах
3. Обновите скрипт инициализации данных

### Добавление новых API endpoints
1. Создайте новый файл в `routes/` или добавьте в существующий
2. Подключите маршрут в `server.js`
3. Обновите документацию

## 🚀 Деплой

1. Установите зависимости: `npm install --production`
2. Настройте переменные окружения
3. Запустите: `npm start`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к файлам данных
4. Убедитесь, что порт 3001 свободен


