═══════════════════════════════════════════════════════════════
🚀 СЛЕДУЮЩИЕ ШАГИ ДЕПЛОЯ
═══════════════════════════════════════════════════════════════

Вы уже создали .env и подключились к БД. Продолжаем!

═══════════════════════════════════════════════════════════════
ШАГ 1: СОЗДАНИЕ .env.local ДЛЯ FRONTEND (если еще не создан)
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
nano .env.local

# Скопируйте и вставьте:
NEXT_PUBLIC_API_URL=http://45.141.78.168:3001/api

# Сохраните: Ctrl+O, Enter, Ctrl+X

═══════════════════════════════════════════════════════════════
ШАГ 2: УСТАНОВКА ЗАВИСИМОСТЕЙ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Установка зависимостей frontend
npm install --production=false

# Установка зависимостей backend
cd backend && npm install && cd ..

# Это может занять несколько минут. Дождитесь завершения.

═══════════════════════════════════════════════════════════════
ШАГ 3: ПРИМЕНЕНИЕ СХЕМЫ БАЗЫ ДАННЫХ
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Применение схемы (создание таблиц)
PGPASSWORD=27061406 psql -h localhost -U magazin_user -d magazin -f backend/database/schema.postgres.sql

# Проверка создания таблиц
PGPASSWORD=27061406 psql -h localhost -U magazin_user -d magazin -c "\dt"

# Должны быть созданы таблицы:
# - users
# - categories
# - products
# - orders
# - order_items
# - cart_items
# - telegram_settings

═══════════════════════════════════════════════════════════════
ШАГ 4: СБОРКА FRONTEND
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store
npm run build

# Это может занять 2-5 минут. Дождитесь завершения.
# В конце должно быть: "✓ Compiled successfully"

═══════════════════════════════════════════════════════════════
ШАГ 5: СОЗДАНИЕ АДМИНИСТРАТОРА
═══════════════════════════════════════════════════════════════

# Создание директории для логов
mkdir -p logs

# Создание администратора
cd backend
source ../backend/.env 2>/dev/null || true
node scripts/create-admin-user.js
cd ..

# Результат: будет создан администратор
# Email: admin@example.com
# Пароль: admin123

═══════════════════════════════════════════════════════════════
ШАГ 6: ЗАПУСК ЧЕРЕЗ PM2
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store

# Остановка старых процессов (если есть)
pm2 delete all 2>/dev/null || true

# Запуск приложений
pm2 start ecosystem.config.js

# Сохранение конфигурации
pm2 save

# Настройка автозапуска
pm2 startup
# Выполните команду, которую PM2 выведет (обычно что-то вроде):
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root

═══════════════════════════════════════════════════════════════
ШАГ 7: ПРОВЕРКА РАБОТЫ
═══════════════════════════════════════════════════════════════

# Статус PM2 (должны быть запущены 2 процесса)
pm2 status

# Логи (последние 20 строк)
pm2 logs --lines 20

# Проверка API
curl http://localhost:3001/api/health

# Должен вернуть: {"status":"OK","timestamp":"...","uptime":...}

# Проверка в браузере:
# http://45.141.78.168:3000
# http://45.141.78.168:3000/admin

═══════════════════════════════════════════════════════════════
ВСЕ В ОДНОЙ КОМАНДЕ (после создания .env.local)
═══════════════════════════════════════════════════════════════

cd /opt/asian-goods-store && \
npm install --production=false && \
cd backend && npm install && cd .. && \
PGPASSWORD=27061406 psql -h localhost -U magazin_user -d magazin -f backend/database/schema.postgres.sql && \
npm run build && \
mkdir -p logs && \
cd backend && source ../backend/.env 2>/dev/null || true && node scripts/create-admin-user.js && cd .. && \
pm2 delete all 2>/dev/null || true && \
pm2 start ecosystem.config.js && \
pm2 save && \
pm2 status

═══════════════════════════════════════════════════════════════
ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК
═══════════════════════════════════════════════════════════════

# Проверка логов backend
pm2 logs asian-goods-backend --err --lines 50

# Проверка логов frontend
pm2 logs asian-goods-frontend --err --lines 50

# Перезапуск всех процессов
pm2 restart all

# Проверка подключения к БД
PGPASSWORD=27061406 psql -h localhost -U magazin_user -d magazin -c "SELECT 1;"

# Проверка переменных окружения
cat backend/.env

═══════════════════════════════════════════════════════════════
ДАННЫЕ ДЛЯ ВХОДА В АДМИН-ПАНЕЛЬ
═══════════════════════════════════════════════════════════════

Email: admin@example.com
Пароль: admin123

⚠️ ВАЖНО: Смените пароль после первого входа!

═══════════════════════════════════════════════════════════════

