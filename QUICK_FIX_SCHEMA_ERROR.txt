═══════════════════════════════════════════════════════════════
🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ: Ошибка schema.sql
═══════════════════════════════════════════════════════════════

Проблема: Backend пытается открыть schema.sql (старый SQLite файл)
Ошибка: ENOENT: no such file or directory, open 'schema.sql'

═══════════════════════════════════════════════════════════════
ВАРИАНТ 1: АВТОМАТИЧЕСКОЕ ИСПРАВЛЕНИЕ
═══════════════════════════════════════════════════════════════

В PowerShell выполните:

.\fix-server-database.ps1

Скрипт автоматически:
- Загрузит правильный database.js
- Загрузит schema.postgres.sql (если нет)
- Загрузит postgres.js (если нет)
- Удалит старый schema.sql
- Перезапустит backend

═══════════════════════════════════════════════════════════════
ВАРИАНТ 2: РУЧНОЕ ИСПРАВЛЕНИЕ
═══════════════════════════════════════════════════════════════

ШАГ 1: На вашем компьютере - загрузите правильные файлы
───────────────────────────────────────────────────────────────

# В PowerShell (из директории проекта):
scp backend/database/database.js root@45.141.78.168:/opt/asian-goods-store/backend/database/database.js

scp backend/database/postgres.js root@45.141.78.168:/opt/asian-goods-store/backend/database/postgres.js

scp backend/database/schema.postgres.sql root@45.141.78.168:/opt/asian-goods-store/backend/database/schema.postgres.sql

───────────────────────────────────────────────────────────────

ШАГ 2: На сервере - удалите старый schema.sql
───────────────────────────────────────────────────────────────

ssh root@45.141.78.168
cd /opt/asian-goods-store
rm -f backend/database/schema.sql

───────────────────────────────────────────────────────────────

ШАГ 3: На сервере - проверьте файлы
───────────────────────────────────────────────────────────────

ls -la backend/database/

# Должны быть:
# - database.js (новый, использует postgres.js)
# - postgres.js
# - schema.postgres.sql
# - НЕ должно быть: schema.sql

───────────────────────────────────────────────────────────────

ШАГ 4: На сервере - перезапустите backend
───────────────────────────────────────────────────────────────

pm2 restart asian-goods-backend
pm2 status

# Backend должен быть "online" (зеленый)

───────────────────────────────────────────────────────────────

ШАГ 5: Проверьте логи
───────────────────────────────────────────────────────────────

pm2 logs asian-goods-backend --lines 20

# Не должно быть ошибок про schema.sql
# Должно быть: "✅ Подключение к PostgreSQL установлено"

───────────────────────────────────────────────────────────────

ШАГ 6: Проверьте работу API
───────────────────────────────────────────────────────────────

curl http://localhost:3001/api/health

# Должен вернуть: {"status":"OK",...}

───────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════
ПРОВЕРКА ПОСЛЕ ИСПРАВЛЕНИЯ
═══════════════════════════════════════════════════════════════

1. pm2 status
   → Backend должен быть "online" (зеленый)

2. pm2 logs asian-goods-backend --lines 10
   → Не должно быть ошибок про schema.sql
   → Должно быть: "✅ Подключение к PostgreSQL установлено"

3. curl http://45.141.78.168:3001/api/health
   → Должен вернуть {"status":"OK"}

4. Попробуйте войти в личный кабинет
   → Должно работать

═══════════════════════════════════════════════════════════════
ЕСЛИ ПРОБЛЕМА ОСТАЛАСЬ
═══════════════════════════════════════════════════════════════

1. Проверьте содержимое database.js на сервере:
   cat /opt/asian-goods-store/backend/database/database.js
   
   Должно быть:
   const pg = require('./postgres');
   ...
   async init() {
     return pg.init();
   }

2. Проверьте наличие postgres.js:
   ls -la /opt/asian-goods-store/backend/database/postgres.js

3. Проверьте настройки PostgreSQL в .env:
   cat /opt/asian-goods-store/backend/.env | grep PG

4. Попробуйте запустить backend напрямую:
   cd /opt/asian-goods-store
   node backend/server.js
   
   Это покажет ошибку в реальном времени.

═══════════════════════════════════════════════════════════════

